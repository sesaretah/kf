<%= form_for(@order_status, html: {class: 'card'}) do |f| %>
<div class="card-header">
  <h3 class="card-title"><%=t :new_order_status %></h3>
</div>
<% if @order_status.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@order_status.errors.count, "error") %> prohibited this order_status from being saved:</h2>

  <ul>
    <% @order_status.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="card-body">
<div class="row">
  <div class="col-md-5">
    <div class="form-group">
          <%= f.label :title, class: "form-label" %>
      <%= f.text_field :title , class: "form-control" %>
          </div>
  </div>
</div>
<div class="card-footer text-right">
  <button type="submit" class="btn btn-primary"><%=t :submit%></button>
</div>
<% end %>
